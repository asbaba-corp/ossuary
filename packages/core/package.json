{
  "name": "@ossuary/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.build.json && node --test test/*.test.js",
    "scenario": "pnpm build && node dist/vertical-scenario.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
